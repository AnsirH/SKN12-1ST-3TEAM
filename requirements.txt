# 전국 차량 등록 현황 분석 대시보드 - 프로젝트 의존성 패키지
# ============================================================
#
# 이 파일은 프로젝트 실행에 필요한 Python 패키지들과 버전을 정의합니다.
# 각 패키지는 차량 등록 현황 분석 및 시각화에 필요한 기능을 제공합니다.
#
# 주석 작성일: 2025.09.01
#
# 패키지 설치 방법:
# pip install -r requirements.txt

# 데이터 처리 및 분석
pandas == 2.2.2          # 데이터프레임 조작 및 데이터 분석 (CSV 읽기, 데이터 전처리 등)
numpy == 1.26.4          # 수치 계산 및 배열 처리

# 웹 애플리케이션 프레임워크
streamlit == 1.43.2      # 대시보드 UI 구축 및 웹 애플리케이션 개발

# 지도 시각화
pydeck == 0.9.1          # 지도 기반 데이터 시각화 (전국 차량 등록 현황 지도 표시)

# 데이터 시각화
seaborn == 0.13.2        # 통계적 데이터 시각화 (성별/연령별 차량 등록 현황 차트)
matplotlib == 3.8.4      # 그래프 라이브러리 (seaborn의 백엔드)
altair == 5.2.0          # 인터랙티브 차트 생성

# 엑셀 파일 처리
openpyxl == 3.1.5        # Excel 파일 읽기/쓰기 (위치 정보 데이터 로드)

# 데이터베이스 연결
mysql-connector-python == 8.3.0  # MySQL 데이터베이스 연결 및 쿼리 실행

# CSV 파일 처리
csv == 1.0               # CSV 파일 읽기/쓰기 (Python 기본 라이브러리)

# 날짜/시간 처리
datetime == 5.4          # 날짜 및 시간 데이터 처리 (Python 기본 라이브러리)

# 시스템 및 파일 처리
importlib == 3.12        # 동적 모듈 로딩 (Python 기본 라이브러리)